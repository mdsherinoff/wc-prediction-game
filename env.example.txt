# --- Database (Neon, Supabase, Railway, or any Postgres) ---
# Get this from your Postgres provider. Must include ?sslmode=require for most hosted providers.
DATABASE_URL="postgresql://user:password@host:5432/worldcup?sslmode=require"

# --- Auth.js (NextAuth v5) ---
# Generate with: npx auth secret
# or: openssl rand -base64 33
AUTH_SECRET="replace-with-generated-secret"

# Your deployed URL (no trailing slash). For local dev: http://localhost:3000
AUTH_URL="http://localhost:3000"

# --- Google OAuth ---
# From Google Cloud Console > APIs & Services > Credentials
# Authorized redirect URI must be: {AUTH_URL}/api/auth/callback/google
AUTH_GOOGLE_ID="your-google-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# --- Restrict signups (optional but recommended for a friends-only group) ---
# Comma-separated list of allowed emails. Leave blank to allow any Google account.
ALLOWED_EMAILS="friend1@gmail.com,friend2@gmail.com,you@gmail.com"

# --- Admin access ---
# Comma-separated list of emails allowed to access /admin (enter results, sync, etc.)
ADMIN_EMAILS="you@gmail.com"

# --- football-data.org API (free tier) ---
# Sign up free at https://www.football-data.org/client/register
# Free tier: 10 calls/minute, includes FIFA World Cup competition (code: WC)
FOOTBALL_DATA_API_KEY="your-football-data-org-key"

# Secret token to protect the manual/cron sync endpoint from public calls
# (used if you wire up your own external scheduler instead of Vercel Cron)
SYNC_SECRET="pick-a-random-string"

# Used by Vercel's built-in Cron Jobs (see vercel.json). Vercel automatically
# sends this as "Authorization: Bearer <CRON_SECRET>" on the scheduled call.
# Generate any random string of 16+ characters.
CRON_SECRET="pick-another-random-string"
